/**
 * Created by vpl on 2017-07-10.
 */


function calcArea() {
    var height;
    var width;
    var area;

    height = 15;
    width = 25;
    area = height * width;

    alert("The area height: " + height + "w: " + width + " a: " + area );
}